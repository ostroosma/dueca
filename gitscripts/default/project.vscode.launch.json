{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug solo",
            "type": "gdb",
            "request": "launch",
            "target": "${workspaceFolder}/build/dueca_run.x",
            "cwd": "${workspaceFolder}/run/solo/solo"
        }
    ]
}
